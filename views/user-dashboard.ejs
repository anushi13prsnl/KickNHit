<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'green-600': '#059669',
                        'green-700': '#047857'
                    }
                }
            }
        }
    </script>
</head>

<body class="min-h-screen bg-gray-50">

    <!-- Navbar -->
    <nav class="bg-white shadow-sm border-b sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center">
                    <h1 class="text-2xl font-bold text-green-600">KickNHit</h1>
                </div>

                <div class="hidden md:flex items-center space-x-8">
                    <a href="#products" class="text-gray-700 hover:text-green-600 font-medium">📦Products</a>
                    <a href="#cart" class="text-gray-700 hover:text-green-600 font-medium relative">
                        🛒Cart
                        <span id="cart-count"
                            class="absolute -top-2 -right-2 bg-red-500 text-white text-xs rounded-full h-5 w-5 flex items-center justify-center">0</span>
                    </a>
                </div>

                <div class="flex items-center space-x-4">
                    <span class="text-gray-700">Welcome, <strong>
                            <%= user.firstName %>
                        </strong></span>
                    <form action="/logout" method="POST" class="inline">
                        <button type="submit"
                            class="px-4 py-2 border border-gray-300 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-50 transition-colors">
                            Logout
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="bg-gradient-to-br from-green-50 to-blue-50 py-16">
        <div
            class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-col-reverse md:flex-row items-center justify-between">

            <div class="text-center md:text-left md:w-1/2">
                <h1 class="text-4xl sm:text-5xl font-extrabold text-gray-700 mb-4">
                    Welcome to Your Sports Store,
                    <span class="text-green-600">
                        <%= user.firstName %>!
                    </span>
                </h1>
                <p class="text-xl text-gray-700 mb-6">
                    Discover premium cricket and football equipment with exclusive deals.
                </p>
                <p class="text-md text-gray-600">
                    Simply click <span class="font-semibold">"Add to Cart"</span> on any item you like.<br>
                    Track your selections in the cart and checkout when ready!
                </p>
            </div>

            <div class="md:w-1/2 mb-10 md:mb-0">
                <img src="https://media.istockphoto.com/id/520703373/photo/man-looking-to-buy-jacket-and-outdoor-equipment.jpg?s=612x612&w=0&k=20&c=foS1JrzI9m0M03GjRWSmMU_PqKcPCRjZRm3NQPcPMtA="
                    alt="Man shopping for sports gear" class="rounded-2xl shadow-lg w-full h-auto object-cover" />
            </div>

        </div>
    </section>

    <!-- stats -->
     <section class="py-2 bg-green-600">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid grid-cols-2 md:grid-cols-4 gap-8 text-center">
                <div>
                    <div class="text-3xl md:text-4xl font-bold text-white mb-2">10K+</div>
                    <div class="text-green-100">Happy Customers</div>
                </div>
                <div>
                    <div class="text-3xl md:text-4xl font-bold text-white mb-2">500+</div>
                    <div class="text-green-100">Products</div>
                </div>
                <div>
                    <div class="text-3xl md:text-4xl font-bold text-white mb-2">50+</div>
                    <div class="text-green-100">Brands</div>
                </div>
                <div>
                    <div class="text-3xl md:text-4xl font-bold text-white mb-2">24/7</div>
                    <div class="text-green-100">Support</div>
                </div>
            </div>
        </div>
    </section>


    <!-- Product Categories Section -->
    <section id="products" class="py-16 bg-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold text-gray-900 mb-4">Shop by Category</h2>
                <p class="text-gray-600">Find exactly what you need for your sport. Browse our extensive collection of professional-grade cricket and football equipment, designed for players at every level.</p>
            </div>
        </div>

        <!-- Category Tabs -->
        <div class="flex justify-center mb-8">
            <div class="bg-gray-100 rounded-lg p-1 flex space-x-1">
                <button onclick="showCategory('cricket')" id="cricket-</div>tab"
                    class="px-6 py-2 rounded-lg bg-green-600 text-white font-medium transition-colors">
                    🏏 Cricket
            </div> </button>
            <button onclick="showCategory('football')" id="football-tab"
                class="px-6 py-2 rounded-lg text-gray-600 hover:text-gray-900 font-medium transition-colors">
                ⚽ Football
            </button>
        </div>
        </div>

        <!-- Cricket Products -->
        <div id="cricket-products" class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <!-- Products will be loaded dynamically from API -->
        </div>

        <!-- Football Products -->
        <div id="football-products" class="hidden grid grid-cols-1 md:grid-cols-3 gap-8">
            <!-- Products will be loaded dynamically from API -->
        </div>
        </div>
    </section>

    <!-- Featured Products Section -->
    <section id="featured" class="py-16 bg-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <!-- <h2 class="text-3xl font-bold text-gray-900 mb-4">Our Featured Products</h2> -->
                <p class="text-gray-600">Hand-picked favorites loved by professionals</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <!-- Featured Cricket Bat -->
                <div class="bg-white rounded-lg shadow-lg hover:shadow-xl transition-shadow border-2 border-yellow-300">
                    <div class="relative">
                        <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTVi_V4PxSEsCrR_qmOF9MtDzxJnKpBI3-ikg&s"
                            alt="Professional Cricket Bat" class="w-full h-48 object-cover rounded-t-lg">
                        <div class="absolute top-2 left-2 bg-yellow-400 text-black px-2 py-1 rounded text-xs font-bold">
                            FEATURED</div>
                    </div>
                    <div class="p-6">
                        <h3 class="text-lg font-semibold mb-2">Professional Cricket Bat Pro</h3>
                        <p class="text-gray-600 mb-4">Premium English willow, used by international players</p>
                        <div class="flex justify-between items-center">
                            <span class="text-2xl font-bold text-green-600">₹4,999</span>
                            <div class="flex justify-end">
                                <button onclick="addToCart('featured-bat', 'Professional Cricket Bat Pro', 4999)"
                                    class="px-3 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors text-sm">
                                    Add to Cart
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Featured Football Boots -->
                <div class="bg-white rounded-lg shadow-lg hover:shadow-xl transition-shadow border-2 border-yellow-300">
                    <div class="relative">
                        <img src="https://i.ebayimg.com/images/g/wqwAAOSwU0JjFook/s-l1200.webp"
                            alt="Professional Football Boots" class="w-full h-48 object-cover rounded-t-lg">
                        <div class="absolute top-2 left-2 bg-yellow-400 text-black px-2 py-1 rounded text-xs font-bold">
                            FEATURED</div>
                    </div>
                    <div class="p-6">
                        <h3 class="text-lg font-semibold mb-2">Elite Football Boots</h3>
                        <p class="text-gray-600 mb-4">Professional grade boots with advanced grip technology</p>
                        <div class="flex justify-between items-center">
                            <span class="text-2xl font-bold text-green-600">₹5,999</span>
                            <div class="flex justify-end">
                                <button onclick="addToCart('featured-boots', 'Elite Football Boots', 5999)"
                                    class="px-3 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors text-sm">
                                    Add to Cart
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Featured Kit -->
                <div class="bg-white rounded-lg shadow-lg hover:shadow-xl transition-shadow border-2 border-yellow-300">
                    <div class="relative">
                        <img src="https://cdnmedia.dsc-cricket.com/media/catalog/product/cache/5b0ea239e50527b43e3253a7f103e237/d/s/dsc-2020-cricket-kit_2.webp"
                            alt="Complete Cricket Kit" class="w-full h-48 object-cover rounded-t-lg">
                        <div class="absolute top-2 left-2 bg-yellow-400 text-black px-2 py-1 rounded text-xs font-bold">
                            FEATURED</div>
                    </div>
                    <div class="p-6">
                        <h3 class="text-lg font-semibold mb-2">Complete Cricket Kit</h3>
                        <p class="text-gray-600 mb-4">Everything you need: bat, pads, gloves, helmet</p>
                        <div class="flex justify-between items-center">
                            <span class="text-2xl font-bold text-green-600">₹8,999</span>
                            <div class="flex justify-end">
                                <button onclick="addToCart('featured-kit', 'Complete Cricket Kit', 8999)"
                                    class="px-3 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors text-sm">
                                    Add to Cart
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Sale Section -->
    <section id="sale" class="py-16 bg-red-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold text-gray-900 mb-4">Flash Sale - Limited Time!</h2>
                <p class="text-gray-600">Up to 40% off on selected items</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
                <!-- Sale Item 1 -->
                <div class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow border-2 border-red-300">
                    <div class="relative">
                        <img src="https://a2cricket.com/cdn/shop/files/A2Cricket-CricketBattingPads_7_1400x.jpg?v=1690984808"
                            alt="Cricket Pads" class="w-full h-40 object-cover rounded-t-lg">
                        <div class="absolute top-2 left-2 bg-red-500 text-white px-2 py-1 rounded text-xs font-bold">40%
                            OFF</div>
                    </div>
                    <div class="p-4">
                        <h3 class="text-md font-semibold mb-2">Cricket Batting Pads</h3>
                        <div class="flex justify-between items-center">
                            <div>
                                <span class="text-lg font-bold text-red-600">₹1,199</span>
                                <span class="text-sm text-gray-500 line-through ml-2">₹1,999</span>
                            </div>
                            <button onclick="addToCart('sale-pads', 'Cricket Batting Pads', 1199)"
                                class="px-3 py-2 bg-red-600 text-white rounded text-sm hover:bg-red-700">
                                Add to Cart
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Sale Item 2 -->
                <div class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow border-2 border-red-300">
                    <div class="relative">
                        <img src="https://contents.mediadecathlon.com/p2571328/bcd3c25846279cde8d74b9996aef2566/p2571328.jpg?format=auto&quality=70&f=2520x0"
                            alt="Football" class="w-full h-40 object-cover rounded-t-lg">
                        <div class="absolute top-2 left-2 bg-red-500 text-white px-2 py-1 rounded text-xs font-bold">35%
                            OFF</div>
                    </div>
                    <div class="p-4">
                        <h3 class="text-md font-semibold mb-2">Professional Football</h3>
                        <div class="flex justify-between items-center">
                            <div>
                                <span class="text-lg font-bold text-red-600">₹1,299</span>
                                <span class="text-sm text-gray-500 line-through ml-2">₹1,999</span>
                            </div>
                            <button onclick="addToCart('sale-football', 'Professional Football', 1299)"
                                class="px-3 py-2 bg-red-600 text-white rounded text-sm hover:bg-red-700">
                                Add to Cart
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Sale Item 3 -->
                <div class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow border-2 border-red-300">
                    <div class="relative">
                        <img src="https://sportsgoodsmarket.com/wp-content/uploads/2020/12/PRO-5-scaled.jpg"
                            alt="Cricket Gloves" class="w-full h-40 object-cover rounded-t-lg">
                        <div class="absolute top-2 left-2 bg-red-500 text-white px-2 py-1 rounded text-xs font-bold">30%
                            OFF</div>
                    </div>
                    <div class="p-4">
                        <h3 class="text-md font-semibold mb-2">Cricket Batting Gloves</h3>
                        <div class="flex justify-between items-center">
                            <div>
                                <span class="text-lg font-bold text-red-600">₹629</span>
                                <span class="text-sm text-gray-500 line-through ml-2">₹899</span>
                            </div>
                            <button onclick="addToCart('sale-gloves', 'Cricket Batting Gloves', 629)"
                                class="px-3 py-2 bg-red-600 text-white rounded text-sm hover:bg-red-700">
                                Add to Cart
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Sale Item 4 -->
                <div class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow border-2 border-red-300">
                    <div class="relative">
                        <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTFFjrb83_2blLq98U-oonOUB3NSU5JaX3-Dw&s"
                            alt="Cricket Helmet" class="w-full h-40 object-cover rounded-t-lg">
                        <div class="absolute top-2 left-2 bg-red-500 text-white px-2 py-1 rounded text-xs font-bold">25%
                            OFF</div>
                    </div>
                    <div class="p-4">
                        <h3 class="text-md font-semibold mb-2">Cricket Helmet</h3>
                        <div class="flex justify-between items-center">
                            <div>
                                <span class="text-lg font-bold text-red-600">₹974</span>
                                <span class="text-sm text-gray-500 line-through ml-2">₹1,299</span>
                            </div>
                            <button onclick="addToCart('sale-helmet', 'Cricket Helmet', 974)"
                                class="px-3 py-2 bg-red-600 text-white rounded text-sm hover:bg-red-700">
                                Add to Cart
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Cart Section -->
    <section id="cart" class="py-16 bg-gray-50">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
            <!-- <h2 class="text-3xl font-bold text-gray-900 mb-8 text-center">Shopping Cart</h2> -->

            <div class="grid grid-cols-1 lg:grid-cols-13 gap-6">
                <!-- Cart Items -->
                <div class="lg:col-span-2">
                    <div id="cart-items" class="space-y-4">
                        <div class="text-center py-12 text-gray-500">
                            <div class="text-6xl mb-4">🛒</div>
                            <p>Your cart is empty</p>
                            <p class="text-sm">Add some products to get started!</p>
                        </div>
                    </div>
                </div>

                <!-- Order Summary -->
                <div class="lg:col-span-2">
                    <div class="bg-white rounded-lg shadow-md p-6 sticky top-24">
                        <h3 class="text-lg font-semibold mb-4">Order Summary</h3>
                        <div class="space-y-2 mb-4">
                            <div class="flex justify-between">
                                <span>Subtotal:</span>
                                <span id="subtotal">₹0</span>
                            </div>
                            <div class="flex justify-between text-green-600">
                                <span>Shipping:</span>
                                <span>FREE</span>
                            </div>
                            <div class="border-t pt-2 flex justify-between font-bold">
                                <span>Total:</span>
                                <span id="total">₹0</span>
                            </div>
                        </div>
                        <div style="display: flex; justify-content: center;">
                            <button id="checkout-btn" onclick="checkout()"
                                class="w-1/4 bg-green-600 text-white py-3 rounded-lg hover:bg-green-700 transition-colors disabled:bg-gray-300"
                                disabled>
                                Proceed to Checkout
                            </button>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Success Modal -->
    <div id="success-modal" class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50">
        <div class="bg-white rounded-lg p-8 max-w-md mx-4">
            <div class="text-center">
                <div class="text-6xl mb-4">🎉</div>
                <h3 class="text-2xl font-bold mb-4">Order Placed Successfully!</h3>
                <p class="text-gray-600 mb-6">Thank you for shopping with KickNHit. Your order will be delivered soon!
                </p>
                <button onclick="closeModal()"
                    class="px-6 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors">
                    Continue Shopping
                </button>
            </div>
        </div>
    </div>


    <script>
        let cart = [];
        let allProducts = { cricket: [], football: [] };

        // Load products from API
        async function loadProducts() {
            try {
                const response = await fetch('/api/products');
                const data = await response.json();
                
                if (data.success) {
                    // Organize products by category
                    allProducts.cricket = data.products.filter(p => p.category === 'cricket');
                    allProducts.football = data.products.filter(p => p.category === 'football');
                    
                    // Display current category
                    const currentCategory = document.getElementById('cricket-products').classList.contains('hidden') ? 'football' : 'cricket';
                    displayProducts(currentCategory);
                } else {
                    console.error('Failed to load products:', data.message);
                }
            } catch (error) {
                console.error('Error loading products:', error);
            }
        }

        // Display products for a category
        function displayProducts(category) {
            const container = document.getElementById(`${category}-products`);
            const products = allProducts[category];
            
            if (products.length === 0) {
                container.innerHTML = `
                    <div class="col-span-full text-center py-12 text-gray-500">
                        <div class="text-6xl mb-4">📦</div>
                        <p>No ${category} products available</p>
                        <p class="text-sm">Check back later for new arrivals!</p>
                    </div>
                `;
                return;
            }

            container.innerHTML = products.map(product => `
                <div class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow border" data-product-id="${product._id}">
                    <div class="relative">
                        <img src="${product.image}" 
                             alt="${product.name}" 
                             class="w-full h-48 object-cover rounded-t-lg"
                             onerror="this.src='https://via.placeholder.com/300x200?text=Image+Not+Found'">
                        ${product.createdAt && new Date(product.createdAt) > new Date(Date.now() - 7*24*60*60*1000) ? 
                            `<div class="absolute top-2 left-2 bg-green-500 text-white px-2 py-1 rounded text-xs font-bold">NEW</div>` : ''}
                    </div>
                    <div class="p-6">
                        <h3 class="text-lg font-semibold mb-2">${product.name}</h3>
                        <p class="text-gray-600 mb-4">${product.description}</p>
                        <div class="flex justify-between items-center">
                            <span class="text-2xl font-bold text-green-600">₹${product.price}</span>
                            <div class="flex justify-end">
                                <button onclick="addToCart('${product._id}', '${product.name}', ${product.price})"
                                    class="px-3 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors text-sm">
                                    Add to Cart
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            `).join('');
        }

        function showCategory(category) {
            // Update tab styling
            document.getElementById('cricket-tab').className = category === 'cricket'
                ? 'px-6 py-2 rounded-lg bg-green-600 text-white font-medium transition-colors'
                : 'px-6 py-2 rounded-lg text-gray-600 hover:text-gray-900 font-medium transition-colors';

            document.getElementById('football-tab').className = category === 'football'
                ? 'px-6 py-2 rounded-lg bg-green-600 text-white font-medium transition-colors'
                : 'px-6 py-2 rounded-lg text-gray-600 hover:text-gray-900 font-medium transition-colors';

            // Show/hide product sections
            if (category === 'cricket') {
                document.getElementById('cricket-products').classList.remove('hidden');
                document.getElementById('cricket-products').classList.add('grid');
                document.getElementById('football-products').classList.add('hidden');
                document.getElementById('football-products').classList.remove('grid');
            } else {
                document.getElementById('football-products').classList.remove('hidden');
                document.getElementById('football-products').classList.add('grid');
                document.getElementById('cricket-products').classList.add('hidden');
                document.getElementById('cricket-products').classList.remove('grid');
            }
            
            // Display products for the selected category
            displayProducts(category);
        }

        // Refresh products periodically to sync with admin changes
        function startProductSync() {
            setInterval(async () => {
                await loadProducts();
            }, 30000); // Refresh every 30 seconds
        }



        function addToCart(id, name, price) {
            const existingItem = cart.find(item => item.id === id);
            if (existingItem) {
                existingItem.quantity += 1;
            } else {
                cart.push({ id, name, price, quantity: 1 });
            }
            updateCart();
            showNotification(`${name} added to cart!`);
        }

        function removeFromCart(id) {
            cart = cart.filter(item => item.id !== id);
            updateCart();
        }

        function updateQuantity(id, quantity) {
            const item = cart.find(item => item.id === id);
            if (item) {
                item.quantity = Math.max(1, quantity);
                updateCart();
            }
        }

        function updateCart() {
            const cartCount = document.getElementById('cart-count');
            const cartItems = document.getElementById('cart-items');
            const subtotal = document.getElementById('subtotal');
            const total = document.getElementById('total');
            const checkoutBtn = document.getElementById('checkout-btn');

            const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);
            cartCount.textContent = totalItems;

            if (cart.length === 0) {
                cartItems.innerHTML = `
                    <div class="text-center py-12 text-gray-500">
                        <div class="text-6xl mb-4">🛒</div>
                        <p>Your cart is empty</p>
                        <p class="text-sm">Add some products to get started!</p>
                    </div>
                `;
                checkoutBtn.disabled = true;
            } else {
                cartItems.innerHTML = `
                    <div class="text-center mb-6">
                        <div class="text-4xl mb-2">🛒</div>
                        <h3 class="text-lg font-semibold text-gray-800">View Your Cart</h3>
                        <p class="text-sm text-gray-600">Review your selected items</p>
                    </div>
                    ${cart.map(item => `
                        <div class="bg-white rounded-lg shadow-md p-4 flex items-center justify-between">
                            <div class="flex-1">
                                <h4 class="font-semibold">${item.name}</h4>
                                <p class="text-gray-600">₹${item.price}</p>
                            </div>
                            <div class="flex items-center space-x-2">
                                <button onclick="updateQuantity('${item.id}', ${item.quantity - 1})" class="px-2 py-1 bg-gray-200 rounded text-sm">-</button>
                                <span class="px-3">${item.quantity}</span>
                                <button onclick="updateQuantity('${item.id}', ${item.quantity + 1})" class="px-2 py-1 bg-gray-200 rounded text-sm">+</button>
                                <button onclick="removeFromCart('${item.id}')" class="px-3 py-1 bg-red-500 text-white rounded text-sm ml-4">Remove</button>
                            </div>
                        </div>
                    `).join('')}
                `;
                checkoutBtn.disabled = false;
            }

            const subtotalAmount = cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
            subtotal.textContent = `₹${subtotalAmount}`;
            total.textContent = `₹${subtotalAmount}`;
        }

        function checkout() {
            if (cart.length === 0) return;

            document.getElementById('success-modal').classList.remove('hidden');
            document.getElementById('success-modal').classList.add('flex');
            cart = [];
            updateCart();
        }

        function closeModal() {
            document.getElementById('success-modal').classList.add('hidden');
            document.getElementById('success-modal').classList.remove('flex');
        }

        function showNotification(message) {
            const notification = document.createElement('div');
            notification.className = 'fixed top-20 right-4 bg-green-500 text-white px-4 py-2 rounded-lg shadow-lg z-50 transition-opacity';
            notification.textContent = message;
            document.body.appendChild(notification);

            setTimeout(() => {
                notification.style.opacity = '0';
                setTimeout(() => notification.remove(), 300);
            }, 2000);
        }

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });

        // Initialize page
        document.addEventListener('DOMContentLoaded', async function () {
            await loadProducts(); // Load products from API
            showCategory('cricket'); // Show cricket products by default
            startProductSync(); // Start periodic sync
        });
    </script>
</body>

</html>