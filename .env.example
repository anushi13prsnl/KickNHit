# Environment Configuration
NODE_ENV=development
PORT=3001

# Database Configuration
MONGODB_URI=

# Session Configuration
# Change this to any unique string (32+ characters recommended)
# Used to encrypt user sessions - safe to customize without breaking the app
SESSION_SECRET=your_super_secret_session_key_change_in_production

# All demo users/admins use the password - 'password' (hashed with bcrypt).
